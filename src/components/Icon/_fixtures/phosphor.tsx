/**
 * STORY FIXTURE — not part of the public API, never exported from the package.
 *
 * The one place stories get icons from, so they read exactly like consumer
 * code. `@phosphor-icons/react` is a devDependency only: components never
 * import an icon library, they size whatever svg they are given.
 */
export {
  Plus, MagnifyingGlass, DownloadSimple, Trash, Bell, X, DotsThree, Star,
  ArrowClockwise, Check, CaretRight, CaretDown, ArrowRight, ArrowSquareOut,
  UploadSimple, Student, UsersThree, ChalkboardTeacher, GraduationCap, BookOpen,
  Exam, Trophy, CurrencyInr, ClockCountdown, CheckCircle, Warning, WarningCircle,
  BookmarkSimple, Notebook, TerminalWindow, TreeStructure, PresentationChart,
  Heart, Paperclip, Info, SealCheck, Prohibit, User, Copy,
} from '@phosphor-icons/react';
