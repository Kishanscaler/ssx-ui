/**
 * "use client" manifest — BATCH M4 (molecules M4). Only the batch M4 agent edits this file.
 */
export default [
  // Controllable query; clear / Escape handlers.
  'components/SearchInput/SearchInput',
  // Draft text + controllable ISO value; mask input / key / paste handlers; caret layout effect.
  'components/DateInput/DateInput',
  // Radix Toggle (selectable) and the ✕ click handler (removable).
  'components/Chip/Chip',
  // Radix ToggleGroup, controllable state, measured travelling indicator.
  'components/SegmentedControl/SegmentedControl',
  // Radix ToggleGroup, controllable state.
  'components/ToggleButtonGroup/ToggleButtonGroup',
];
