/**
 * "use client" manifest — BATCH M1 (molecules M1). Only the batch M1 agent edits this file.
 * Add the dist path (no extension) of each client module you create. See ./core.mjs.
 */
export default [
  // useId + context for the label / help / error wiring; Radix Label.
  'components/Field/Field',
  // Radix DropdownMenu (state, handlers, portal).
  'components/Menu/Menu',
];
